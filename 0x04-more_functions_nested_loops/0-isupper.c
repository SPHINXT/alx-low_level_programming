#include "main.h"

/**
 * _isupper - Function to check for an uppercase alphabet
 *@c: the character to be checked
 * Return: 1 is success
 */

int _isupper(int c)
{
	if (c >= 'A' & c <= 'Z')
	return (1);
}
